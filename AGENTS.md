# AGENTS.md

Single source of truth for anyone or anything (human or AI agent) picking up this project. If you're an AI coding agent reading this: everything in this file is load-bearing, not background color — follow the "Working principles" section as operating rules, not just information.

## Project
**MovIO** (Mobility Optimization Via Intelligent Operations) — a smart campus transportation management system for the Federal University of Technology, Akure (FUTA), Nigeria. Final year software engineering project. Combines NFC-based boarding authentication (via the **TapTrace** device), real-time GPS shuttle tracking, a prepaid Transit Credit wallet, and a centralized analytics dashboard — including a real sustainability/SDG feature, not just a claim.

**The academic report (Chapters 1-3 already written, in `docs/` or attached to the project) is the source of truth for requirements and architecture.** Every code decision should be checked against it. If code needs to diverge from what's written (this has already happened several times — see "Key decisions" below), **update the report to match** — don't let them drift apart. A real person defends this report to human examiners; internal inconsistency between report and system is a defence risk, not a cosmetic issue.

## Who you're working with
A young, ambitious software engineering student at FUTA, also a practicing software engineer in Nigeria. Comfortable with Vite/React/general JS/TS. Newer to Flutter and embedded firmware (ESP32/C++) specifically — these two areas need more explanation, smaller steps, and reasoning narrated rather than handed over as finished code, since the person wants to actually learn them, not just receive output. Working under real time pressure toward a defence date this month (still unconfirmed exact date as of this writing).

## Working principles (apply these as rules, not suggestions)
1. **Realism over assumption.** Every recommendation should be checked against FUTA/Nigeria's actual conditions where it matters (component availability and pricing, network reliability, power stability, local supplier realities) — don't default to generic/US-centric assumptions. When something is genuinely unknown, say so and ask, rather than filling the gap with a plausible-sounding guess.
2. **Technology choices must flag real learning cost.** If a technology choice would require the person to learn something substantial and new, say so explicitly *before* it's adopted, not after. This has already shaped real decisions — e.g., Express was kept over Fastify/NestJS specifically because switching frameworks under deadline pressure wasn't worth the added learning curve; ESP32 was chosen partly *because* it's better embedded-firmware learning value, and that was named explicitly as a reason.
3. **This project has genuine global relevance, not just local scope.** It's explicitly aligned with UN SDG 4 (Quality Education), SDG 9 (Industry, Innovation and Infrastructure), and SDG 11 (Sustainable Cities and Communities) — see Ch.1 §1.1. This isn't decorative: the dashboard's Sustainability Panel (see Milestone 5 in the milestone plan) is a real, working feature demonstrating this, not just supporting prose. Any new feature work should ask whether it reinforces or undermines this framing.
4. **Academic and formal consistency matters as much as working code.** Don't assume journal targets, formatting requirements, or department expectations — these were explicitly researched, not guessed:
   - Primary post-defence journal target: **FUTAJEET** (FUTA Journal of Engineering and Engineering Technology) — open-access, published by FUTA's own School of Engineering and Engineering Technology, biannual (April/November). Home-court advantage; confirm with supervisor whether they'd co-sponsor submission.
   - Secondary target once real evaluation data exists: **Nigerian Journal of Technology (NIJOTECH)** — Scopus-indexed, University of Nigeria, ~$185 APC.
   - Avoid any "project-mill" service promising bundled project-building + paper-writing + publishing — these read as predatory and undermine originality claims.
   - Any Word document (`.docx`) deliverable produced for this project should use minimal formatting — clean, easily editable structure, not heavy styling — except where a document's whole purpose is formal/special presentation.
5. **Faithful-partner standard.** Don't fill gaps in the person's knowledge or the project's status with invented specifics (prices, stakeholder responses, defence dates, fuel-consumption figures, etc.). Where a real answer is needed and unknown, say exactly who to ask or what to check, and treat that as an open item until a real answer comes back — see "Open items" below.
6. **Draw from the actual chapter documents**, not from a generic idea of what a "smart campus transport system" project looks like. Chapters 1-3 already contain specific, real field data (104-student survey, driver interview, SUG meeting) — decisions should stay consistent with what's already been established and evidenced there.


## Repo structure (monorepo)
```
movio/
  backend/    Node.js/Express REST + WebSocket API, PostgreSQL (via Supabase)
  dashboard/  React/Vite admin dashboard
  mobile/     Flutter student application
  firmware/   ESP32 firmware for the TapTrace device
  docs/       Chapter drafts, milestone plan, manual test plan, setup guides
```
One repo, parallel-track development: hardware (`firmware/`) and software (`backend/`, `dashboard/`, `mobile/`) progress independently and merge at integration testing, not sequentially.

## Confirmed technical decisions (and why)
| Decision | Chosen | Why |
|---|---|---|
| Repo structure | Monorepo (`backend/`, `dashboard/`, `mobile/`, `firmware/`, `docs/`) | One clone for examiners/supervisor; deployment platforms (Vercel, Render) support subfolder root directories cleanly, so the earlier concern about monorepo-vs-deployment friction doesn't hold |
| TapTrace hardware | ESP32 (not Raspberry Pi, as Ch.3 originally specified) | Far cheaper and more available in Nigeria; better power/boot characteristics for a vehicle-mounted device; genuinely better embedded-firmware learning value |
| TapTrace <-> backend protocol | Real MQTT (not a simplified HTTPS shortcut that was briefly proposed and explicitly rejected) | Matches Ch.3's original design; person explicitly chose to build the real thing over the simpler alternative |
| MQTT broker | **HiveMQ Cloud** (free "Serverless" tier - confirmed: 100 connections, 10GB/month, TLS required) — not self-hosted Mosquitto as Ch.3 originally specified | Render's free tier serves HTTP, not raw TCP ports, so self-hosting a broker doesn't fit the free deployment stack; a managed broker needs no maintenance and is still genuinely free at this project's scale |
| Backend framework | Express (kept, not switched to Fastify/NestJS) | Already working and tested; switching frameworks under deadline pressure to rebuild something that already works isn't worth it - explicitly flagged per principle #2 |
| Backend language | **TypeScript** (migrating from JS) | Person's explicit choice; stronger for a portfolio-worthy repo |
| Backend package manager | **Yarn** (not npm) | Person's explicit choice, applies to `backend/` and `dashboard/` only - Flutter uses its own `pub` tooling, no yarn equivalent exists there |
| Database | **Supabase** (free Postgres) | Person already has an account (used for the survey app); free tier *pauses* on inactivity rather than deleting data, unlike Render's free Postgres which hard-deletes after 30 days |
| Backend hosting | **Render** free Web Service | Only free option supporting a persistent Node process with WebSocket - accepted tradeoff: 15-min idle spin-down, ~30-60s cold start. For the actual defence demo, run backend locally instead, to remove this risk on the day it matters most |
| Dashboard hosting | **Vercel** free | Same platform already used for the survey app |
| Mobile | Flutter/Dart, Android-only (matches Ch.1 §1.5 scope) | No cloud deployment needed - APK built locally, sideloaded to a physical test device |
| Mobile dev environment | Physical Android phone (not emulator), HP EliteBook x360 1040 G5, 8GB RAM, Linux | Confirmed directly with the person - avoids emulator resource overhead on an 8GB machine |
| Project management | Linear | Milestone plan and issue descriptions are written to paste directly into Linear issues |

## Open items — do not assume answers to these
- **Defence date**: still unconfirmed as of this writing ("this month," no specific date). This is the single number that reshapes the whole timeline.
- **TapTrace parts**: BOM finalized and being ordered as of 2026-07-03 (was the critical-path bottleneck). Confirmed line items: ESP32 dev board (38-pin, CP2102), PN532 NFC, NEO-6M GPS, **SIM7070 4G LTE** module, plus 2S 18650 power path (BMS holder, LM2596/XL6009 regulators, fuse), passives, prototyping tools, and an IP-rated vehicle-mount enclosure. This resolves two of the project-brief's open hardware questions: **ESP32** (not Raspberry Pi) and **SIM7070 4G** (not SIM800L 2G). Firmware/wiring work is still gated on the parts physically arriving; until then the `backend/scripts/simulate-*` tools stand in for the device.
- **Sustainability panel fuel-consumption figure**: needs a real number from FUTA's Transport Unit or the driver contact already established in Ch.3's fieldwork. Ships as an admin-adjustable estimate, clearly labeled, until this arrives.
- **2G network availability** (only relevant if pursuing the optional SIM800L cellular stretch goal for TapTrace) — needs checking with the person's actual network provider before purchasing that module.
- **Supervisor confirmations needed**: exact defence date; written confirmation that a desk-level NFC/GPS proof-of-concept (not a full in-vehicle pilot) is acceptable; whether FUTAJEET submission is a department expectation; any required report template/style.


## Tech stack — don't silently swap these without discussion
- **Backend:** Node.js + Express (TypeScript, feature-based structure under `backend/src/features/*`), raw `pg` (no ORM — deliberate, keeps queries visible/explainable for defence), PostgreSQL hosted on **Supabase** (free tier — chosen over Render's free Postgres because it pauses rather than hard-deletes after inactivity; RLS is enabled on every table with no policies purely to lock out Supabase's auto-generated PostgREST API — the backend connects as the `postgres` role, which bypasses RLS, so authorization is still enforced entirely in Express), Socket.io, JWT + `bcryptjs` (not `bcrypt` — native bindings failed to build in a sandboxed test environment; pure-JS avoids that class of problem entirely), `winston` for colorized structured logging (HTTP access logs piped through it via `morgan`), `class-validator`/`class-transformer` for request DTO validation, a small `AppError` subclass hierarchy (`shared/errors/`) + centralized error middleware, and a consistent `{ success, message, data }` / `{ success, message, errors }` response envelope (`shared/utils/ApiResponse.ts`)
- **Dashboard:** React + Vite + Tailwind, Leaflet + OpenStreetMap. Deploys to **Vercel** (free), root directory `dashboard`
- **Backend deployment:** **Render** free Web Service, root directory `backend`. Known tradeoff: spins down after 15 min idle, ~30-60s cold start on next request. For live defence demos, run the backend locally instead of relying on the deployed cold-start-prone instance.
- **Mobile:** Flutter (Dart), flutter_map + OpenStreetMap, flutter_nfc_kit, geolocator. No cloud deployment — build APK locally, install on a test device.
- **Firmware:** ESP32 (not Raspberry Pi — cost/power reasons; see report §3.4.5 once updated), PN532 (NFC), Neo-6M (GPS), optionally SIM800L/A7670 for cellular MQTT. Flashed locally via USB, no cloud deployment.
- **Device name:** the vehicle-mounted NFC+GPS unit is **TapTrace** everywhere — code, docs, report. Don't reintroduce "Reader Device."
- **Project management:** Linear. Task codes (`BE-3`, `HW-2`, etc.) referenced in `docs/MovIO_Milestone_Plan.md` should match Linear issue titles — keep them traceable both directions.

## Setup / commands
- Backend: `cd backend && yarn install && cp .env.example .env` (fill in `DATABASE_URL` from Supabase + `JWT_SECRET`), then `yarn dev` (runs on `ts-node-dev`, auto-restarts on save). Health check: `GET /health`.
- Backend DB: schema/migrations are run directly against Supabase (SQL editor or `psql`) — `backend/db/schema.sql` is the source of truth, kept in sync by hand, not by an in-repo migration runner.
- Dashboard: `cd dashboard && yarn install && yarn dev`.
- Mobile: `cd mobile && flutter pub get && flutter run` (needs a connected Android device — see mobile dev environment note above).
- Simulate TapTrace hardware before it physically exists: `yarn simulate <trip_id> [nfc_uid]` from `backend/` (runs `scripts/simulate-taptrace.ts` via `ts-node`) - GPS movement, plus one boarding tap every ~15s if `nfc_uid` is given.
- Simulate several distinct students boarding (to demo Trip Monitoring's `count/capacity`, e.g. `3/40`): `yarn simulate:tap <trip_id> <passenger_count> [interval_ms=3000]` from `backend/` (runs `scripts/simulate-tap.ts`) - self-contained, sets up (and reuses on later runs) that many simulated student identities, taps each one in, spaced apart so the count climbs visibly. Independent of `yarn simulate` - run both together if you want GPS movement and boarding happening at once.
- Manage trips directly from the terminal, bypassing the driver-ownership rule that the real API enforces (dev-only "backdoor", talks straight to the DB, never exposed as an endpoint): `yarn trips list [--all]` / `yarn trips vehicles` / `yarn trips drivers` / `yarn trips start <vehicle_id> [route_id] [driver_id]` / `yarn trips end <trip_id>` / `yarn trips end-all` from `backend/` (runs `scripts/manage-trips.ts`). No driver JWT needed - useful for cleaning up trips left active by the simulator scripts above without hunting down whichever test driver started them.
- Full TapTrace stand-in, interactive: `yarn simulate:device` from `backend/` (runs `scripts/simulate-device.ts`) - guides you through picking (or starting) a trip, then drops into a **live `tap>` console** while GPS streams along the route's real stops (~22 km/h) in the background: type a student's number to tap them (backend decides tap-in vs tap-out), `a` to add another simulated student, `s` for status (who's aboard, position, ping count), `e` to end the trip and exit, `d` to detach leaving the trip running. Trip start/end goes through the real REST API as a self-registered Sim Driver, so the `trip:started`/`trip:ended`/`trip:passengers`/`location:update` socket broadcasts all genuinely fire - point the mobile live map or dashboard at the backend and watch it react in real time. Setup prompts can be skipped with flags (`--trip`/`--vehicle`/`--route`/`--passengers`), `--yes` auto-picks everything, and `--auto [--duration <s>]` runs fully hands-free (auto tap-in, ride, tap-out - no console). Colors + prompts use only `node:util`'s `styleText` and `node:readline/promises` - no new dependency. Run `yarn simulate:device --help` for details.

## Testing
Manual testing is the primary strategy here (student-led final year project, tight timeline) — see `docs/MovIO_Manual_Test_Plan.md` for the full test case table, organized by feature area and mapped to the report's functional/non-functional requirements. When you add a feature, add or update the relevant test cases in that file rather than assuming coverage. If you write any automated tests, keep them simple (basic endpoint/unit tests) — don't introduce a heavy test framework under this timeline without discussion.

## Conventions
- SQL: parameterized queries only, never string-concatenated SQL
- Enums modeled as `VARCHAR` + `CHECK` constraints, not native Postgres `ENUM` types (easier to alter later — see comment at top of `backend/db/schema.sql`)
- Commits: Conventional Commits (`feat:`, `fix:`, `docs:`, `chore:`, `refactor:`) — repo goes public after defence, keep history clean
- Every unauthenticated or clearly-incomplete endpoint gets an inline `// TODO` comment explaining what's missing and why it's deferred — these become the honest "known limitations" list in Chapter 4, not something to hide
- Keep code readable over clever — this person needs to explain every line at defence, in several tools they're still learning

## What NOT to do
- Don't invent features beyond the report's functional requirements without flagging it as a scope addition first
- Don't claim things work (a live bus pilot, hardware tested in-vehicle) that haven't actually been run — Chapter 4 needs to report real results, not aspirational ones
- Don't add heavy frameworks/tooling not already in the stack above without discussing first
- Don't skip `docs/MovIO_Manual_Test_Plan.md` when a feature is "done" — done means tested, not just implemented

## Where things stand (keep this current - update at the end of each work session)
- [x] Backend core logic written: auth, boarding (with wallet deduction transaction), tracking. No MQTT client is actually implemented yet despite an earlier note claiming graceful degradation - `boarding.controller.ts` only *references* an eventual MQTT bridge in a comment; building the real client is still open, blocked on TapTrace parts anyway.
- [x] Backend TypeScript migration - done, feature-based structure (`backend/src/features/{auth,boarding,tracking}`, `shared/`, `config/`)
- [x] Backend cross-cutting infra: `winston` colorized logging (morgan piped through it), `AppError` class hierarchy + centralized error middleware, `class-validator`/`class-transformer` DTO validation on every mutating route, consistent `{success,message,data}` response envelope, fail-fast env var validation (`DATABASE_URL`/`JWT_SECRET` required or the process won't start), RLS enabled (no policies) on every Supabase table to lock out the auto-generated PostgREST API
- [x] Backend + dashboard tooling: ESLint + Prettier wired together in both, VS Code autosave/format-on-save/ESLint-fix-on-save configured for the monorepo
- [x] Backend wallet cash top-up (`BE-10`/PSD-147): `POST /api/v1/wallet/topup-cash`, `transport_personnel`-only, in `backend/src/features/wallet/*`. Merged to `main` (local merge, no PR - solo repo). Verified live against Supabase (register/login test users of each role, curl through every acceptance case) - see `docs/MovIO_Manual_Test_Plan.md` §3 (WALLET-01, 03-06).
- [x] Backend complaint endpoints (`BE-8`/PSD-92): `POST /api/v1/complaints` (student), `GET`/`PATCH /api/v1/admin/complaints[/:id]` (admin, status filter + status update), in `backend/src/features/complaints/*` (first real use of `requireRole` + first admin-guarded routes in this codebase). Merged to `main` (local merge, no PR). Verified live the same way - see `docs/MovIO_Manual_Test_Plan.md` §9 (COMPLAINT-01 through 10).
- [x] Backend trip lifecycle (`BE-9`/PSD-146): `POST /api/v1/trips/start` + `POST /api/v1/trips/:id/end`, driver-JWT-only, in `backend/src/features/trips/*`. Prevents a second active trip per vehicle; ending a trip is scoped to the driver who started it. Merged to `main`. Verified live against Supabase - see `docs/MovIO_Manual_Test_Plan.md` §10 (TRIP-01 through 09).
- [x] Backend ridership report (`BE-7`/PSD-91): `GET /api/v1/admin/reports/ridership?groupBy=route|vehicle|day`, admin-JWT-only, in `backend/src/features/reports/*`. Merged to `main`. Counts cross-checked against manual `SELECT COUNT(*) ... GROUP BY` queries on the live DB - see `docs/MovIO_Manual_Test_Plan.md` §11 (REPORT-01 through 07). Seeded `db/seed.sql`'s vehicle (`FUTA-001`) + route (`South Gate to West Gate`) into the live Supabase instance for real, since it was never actually run there before - left in place as the intended fixture.
- [x] Backend REST surface implemented so far: auth, boarding, tracking, wallet top-up, complaints, trip lifecycle, ridership report. `docs/MovIO_Milestone_Plan.md` isn't present in this repo checkout (only in Linear per the note below) so this isn't a claim that every `BE-*` Linear issue is closed - check Linear for what's actually still open. Still confirmed open: the real MQTT client bridging TapTrace devices to the boarding controller (blocked on TapTrace parts).
- [x] Backend `GET /health` made real (was a static `{service}` stub): now runs `SELECT 1` against the live Supabase pool and reports `status: 'ok'|'degraded'`, DB latency, uptime - 503 when the DB is unreachable, 200 otherwise. Logic in `backend/src/shared/utils/health.ts`. Confirmed via `yarn tsc --noEmit`/`yarn build`/`yarn lint` only this session - the DB-down/503 path is reasoned-through, not observed against a live pulled connection.
- [x] Dashboard (`PSD-148`/FE-2, `PSD-149`/FE-4): no longer just a placeholder shell. Added `.env`/`.env.example` (`VITE_API_BASE_URL`), a hand-rolled JWT auth foundation (`src/lib/auth.ts`, `src/lib/api.ts`, `src/contexts/AuthContext.tsx`, `src/hooks/useAuth.ts` - no new npm dependency), a role-gated (`role === 'admin'`) `ProtectedRoute` guard (`src/routes/ProtectedRoute.tsx`) wired into `react-router-dom` routes in `App.tsx`, a minimal `Login` page, a `DashboardLayout` shell (sidebar/topbar, only "Overview" nav item is live, others marked "Coming soon"), and an `Overview` page hosting a `SystemStatusCard` widget that polls `/health` every 15s and distinguishes ok/degraded/slow-to-respond/unreachable. Manual test cases DASH-08 through DASH-10 (status widget states) added to `docs/MovIO_Manual_Test_Plan.md` §5, alongside the existing DASH-07 route-guard case. The login page isn't a dedicated Linear ticket in what was handed to this session - built as the minimum needed to make the route guard testable at all; if a separate FE ticket for the login screen exists in Linear, treat this as satisfying it minimally, not as the final word on it. **Not verified in a live browser this session** (explicitly deferred to the engineer) - typecheck/build/lint all pass, but the actual login → guard → logout → re-guard flow and the DASH-07 "no network leak" claim haven't been watched happen.
- [x] Dashboard admin login + full design-system refactor (`PSD-94`/FE-3, plus an explicit "apply the design brief" ask against `docs/admin_design_brief.md`): PSD-94's three acceptance criteria (wrong-credentials error, correct-credentials store+redirect, Bearer auth on subsequent calls) were already satisfied by the prior session's `Login.tsx` - this session redesigned it visually and built out the rest of the brief against the backend as it actually exists, not the brief's full aspirational screen list. Real, backend-integrated screens added: **Fleet Map** (`Overview.tsx` - Leaflet/OSM, polls `GET /tracking/active` every 5s, pulse-ring marker recency states per the brief's signature element, real empty-state copy), **Complaints** (`Complaints.tsx` - status filter tabs, list, status-update dropdown against the real admin complaints endpoints), **Ridership** (`RidershipReport.tsx` - groupBy selector, table + `recharts` bar chart against the real reports endpoint), **Sustainability** (`Sustainability.tsx` - real trip count from the ridership endpoint × an admin-entered, `localStorage`-persisted fuel-per-trip estimate, explicitly labeled "Estimated · pending confirmed fuel data" per the open item below - starts blank rather than pre-filled with an invented number). Design tokens/fonts/icons from the brief are wired in globally (`index.css` `@theme`, self-hosted `@fontsource/ibm-plex-{sans,mono}`, `lucide-react`), and the real MovIO logo/icon set already checked into `dashboard/public/` (previously unused) is now wired into `index.html` and the sidebar/login screen. **Explicitly deferred, shown as disabled "Soon" nav items**: Vehicle management, Route management, Driver Assignment, and full Trip Monitoring - none have backend CRUD/fields yet (confirmed by reading `backend/src/features/*` before scoping this), so building them for real would mean adding backend work that wasn't asked for; not silently faked. One brief-internal contradiction resolved by judgment: the Color section reserves `sustain-green` "exclusively for the Sustainability panel - nowhere else," but the Complaints section's own screen-by-screen text separately suggests a "muted green" for resolved complaints - resolved-complaint badges use a neutral gray instead, honoring the stricter global rule. `dashboard/package.json` gained `@fontsource/ibm-plex-sans`, `@fontsource/ibm-plex-mono`, `lucide-react`, `recharts`, and `react-is` (recharts' unlisted peer dependency, needed for the Vite/Rolldown build to resolve); `react-leaflet` was bumped `^4.2.1` → `^5.0.0` since v4 peer-depends on React 18 and this project is already on React 19 - no working v4 map code existed yet to break. Manual test rows DASH-01b/c, DASH-02b/c/d, DASH-06b/c, DASH-11/11b added to `docs/MovIO_Manual_Test_Plan.md` §5; DASH-03/04 (vehicle/route creation) explicitly marked N/A - not built this pass. **Verified this session**: `yarn lint` clean, `yarn build`'s `tsc` step passes with no type errors; all real endpoints this UI calls (`/auth/login`, `/tracking/active`, `/admin/complaints`, `/admin/reports/ridership` for all three `groupBy` values) hit live against the local backend + Supabase with a freshly-registered admin JWT and confirmed to match the response shapes coded into `lib/api.ts`. **Not verified**: the full production `vite build` (blocked on `react-is` needing a fresh `yarn install` - the engineer runs installs themselves, not yet confirmed done as of this writing) and all in-browser/visual behavior (explicit standing instruction this session to leave that to the engineer).
- [x] Backend + dashboard: Route CRUD (`PSD-150`/FE-7), Trip Monitoring (`PSD-99`/FE-8), Driver Assignment (`PSD-151`/FE-11) - the three nav items that were previously disabled "Soon" placeholders in `DashboardLayout.tsx` are now real. New backend: `backend/src/features/routes/*` (`GET`/`POST /api/v1/admin/routes`, `PATCH /api/v1/admin/routes/:id` - nested `stops` JSONB array validated with `class-validator`'s `@ValidateNested`/`ArrayMinSize(2)`, the first nested-array DTO in this codebase), `backend/src/features/users/*` (`GET /api/v1/admin/users?role=driver`, generic by-role listing), `backend/src/features/vehicles/*` (`GET /api/v1/admin/vehicles`, `PATCH /api/v1/admin/vehicles/:id/assign-driver`), and an `adminTripsRouter` added to the existing `trips` feature (`GET /api/v1/admin/trips`, joins `boarding_events` for a live `passenger_count` per trip). New dashboard pages: `Routes.tsx`, `TripMonitoring.tsx` (polls every 5s, same convention as the Fleet Map), `DriverAssignment.tsx`. **Scope decisions made explicitly, not silently**: (1) driver assignment is a single nullable `assigned_driver_id UUID REFERENCES users(user_id)` column added to `vehicles` (one current driver per vehicle, not an assignment-history table); (2) PSD-151's "visible or usable at trip start" is satisfied by *visibility* only - Trip Monitoring shows each trip's actual driver, Driver Assignment shows each vehicle's assigned driver, admin cross-checks both - `trips.service.startTrip` (BE-9, already tested/merged) was deliberately left untouched rather than adding assignment enforcement, since there's still no driver-facing mobile UI to make that enforcement meaningful end-to-end; (3) Vehicle CRUD (`FE-6` - create/deactivate vehicles) stays out of scope and the "Vehicles" nav item stays disabled - vehicles are only listed (read-only) as supporting data for Driver Assignment. **Migration required, not yet applied**: `ALTER TABLE vehicles ADD COLUMN IF NOT EXISTS assigned_driver_id UUID REFERENCES users(user_id);` needs to be run against the live Supabase instance before the vehicles/driver-assignment endpoints will work - confirmed live that they currently fail with `column v.assigned_driver_id does not exist` (500) until this runs; the engineer chose to run this migration themselves rather than have it run for them this session. **Verified this session**: `yarn build`/`yarn lint` clean in both `backend/` and `dashboard/`; Route CRUD and Trip Monitoring verified live end-to-end against the real Supabase instance (route created/updated/listed with correct JSONB round-trip, 2-stop-minimum validation rejected, passenger count observed going 0→1 on a real boarding tap and cross-checked against a manual `SELECT COUNT(*) FROM boarding_events`, trip status flip to `completed` observed) - see `docs/MovIO_Manual_Test_Plan.md` §12-14. Auth guards (401/403) verified live for every new admin route, including vehicles/assign-driver, since those don't touch the missing column. **Not verified**: the actual assign/unassign flow (blocked on the pending migration) and all dashboard in-browser/visual behavior (left to the engineer, same standing instruction as prior dashboard sessions). Test fixtures (extra test users, one extra route, one completed test trip + boarding event, a wallet top-up) were left in the live Supabase instance from this verification pass, same precedent as prior sessions' `db/seed.sql` seeding.
- [x] Confirmed since last session: the engineer ran the `assigned_driver_id` migration themselves against live Supabase - Driver Assignment (`PSD-151`/FE-11) is now fully working end-to-end, not just visible-only. ASSIGN-03 through 06 in `docs/MovIO_Manual_Test_Plan.md` §14 (previously "Pending (needs migration)") re-verified live and updated to Pass this session.
- [x] Backend + dashboard: Vehicle CRUD (`PSD-96`/FE-6), Fleet Map live Socket.io subscription (`PSD-95`/FE-5). Verified in the same session that Ridership (`PSD-97`/FE-9) and Complaints (`PSD-98`/FE-10) already fully satisfied their Linear acceptance criteria from the prior "PSD-94 design system refactor" session's work - **no new code was written for those two**, only live re-verification (complaint create→list→PATCH→resolved round-trip, all three ridership `groupBy` values) and doc cross-referencing (`FE-9`/`FE-10` added to test-plan section headers §9/§11, which previously only cited `BE-8`/`BE-7`). Re-implementing already-working, already-tested screens was deliberately avoided as unnecessary regression risk.
  - **Vehicle CRUD**: extended `backend/src/features/vehicles/*` (previously list + assign-driver only) with `POST /api/v1/admin/vehicles` and `PATCH /api/v1/admin/vehicles/:id` - `vehicle_type` validated against `bus|cab|tricycle` via `class-validator`'s `@IsIn(VEHICLE_TYPES)`, duplicate `plate_number` caught via Postgres `23505` and surfaced as a clean 409 (same `DatabaseError` pattern as `trips.service.ts`'s `23503` handling). "Deactivate" is just `PATCH { is_active: false }` - no delete endpoint exists, matching the ticket and the design brief's existing "Deactivate not Delete" button-copy guidance. New `dashboard/src/pages/Vehicles.tsx` (table + inline create/edit form, same structural pattern as `Routes.tsx`), nav item enabled, `/vehicles` route added. **Verified live**: create (valid + rejected-enum + duplicate-plate 409), edit, deactivate-then-confirm-row-still-exists-via-GET, reactivate, 404, 401/403 - see `docs/MovIO_Manual_Test_Plan.md` §15 (VEHICLE-01 through 08). Confirmed the shared `GET /admin/vehicles` endpoint (also used by Driver Assignment) still returns the same shape after this change.
  - **Fleet Map sockets**: `Overview.tsx` previously only polled `GET /tracking/active` every 5s for everything, including position; PSD-95's acceptance criteria explicitly asks for a live Socket.io subscription to `location:update` (the backend has emitted this since the tracking feature was built, but nothing consumed it client-side until now). New `dashboard/src/lib/socket.ts` (a singleton `socket.io-client` connection, same "one shared instance" pattern as the backend's own pg pool) and a `socket.on('location:update', ...)` handler in `Overview.tsx` that merges position updates into the existing trips state by `trip_id`. **Deliberate hybrid, not pure-socket**: the `location:update` payload carries no vehicle/route metadata, so it can move an existing marker but can't introduce one for a trip that just started or remove one for a trip that just ended - the REST poll was kept but slowed from 5s to 20s, now serving only as a lifecycle-reconciliation check, not the position-update mechanism. Added `socket.io-client` to `dashboard/package.json` (matches the backend's `socket.io` major version); engineer ran `yarn install` this session, and `yarn build`/`yarn lint` both pass clean afterward. **Not verified**: actually watching a marker move live in a browser without reloading (this ticket's literal example) - no browser automation tool is available in this environment (checked again this session), so this stays an honest gap, same as every prior dashboard session's in-browser deferral. The backend emit side was confirmed working in a prior session; the frontend socket code is now confirmed to typecheck/build/lint clean, but the actual socket connection has not been watched working end-to-end in a browser.
- [x] Fixed a real incident hit while live-testing: `backend/src/config/db.ts`'s pg `Pool` had no `connectionTimeoutMillis` (pg default is `0` = wait forever), so a transient Supabase pooler blip left requests hanging 17-40s+ instead of failing cleanly (`EAI_AGAIN`/`EAUTHTIMEOUT` in the logs). Added a 5s `connectionTimeoutMillis` with a comment tying it to the incident - reproduced the same stall live during the fix and confirmed it now fails clean at exactly 5s (`503`) and recovers on its own once Supabase responds again, instead of hanging. Not a persistent outage - DNS and raw TCP to the pooler both check out fine; this was pooler-side slowness, and failing fast under that condition is the correct behavior for this project's stated network-reliability context, not a workaround.
- [x] Trip Monitoring's passenger count now shows `count/capacity` (e.g. `3/40`), not just a raw count - `trips.model.ts`'s `listTripsWithPassengerCounts` query and `TripMonitorRow` type (both backend and `dashboard/src/lib/api.ts`) gained `v.capacity`; `TripMonitoring.tsx` renders `{passenger_count}/{capacity}`. Small, contained change, verified live end-to-end (see below).
- [x] New `backend/scripts/simulate-tap.ts` (`yarn simulate:tap <trip_id> <passenger_count> [interval_ms]`) - a standalone boarding-tap simulator, separate from `simulate-taptrace.ts`'s GPS+single-tap loop, for demoing several distinct students boarding a trip so the `count/capacity` display climbs visibly. Self-contained: registers (or, on later runs, logs into) that many deterministic simulated student accounts, links each an NFC credential the same direct-SQL way `db/seed.sql` already documents (no REST endpoint exists for that), tops up each wallet via the real `POST /wallet/topup-cash`, then taps each one in via the real `POST /boarding/authenticate`, spaced `interval_ms` apart (default 3000). Reuses the existing `config/db.ts` pool directly (its `dotenv/config` import means this works standalone without needing `server.ts`). **Verified live**: ran `yarn simulate:tap <trip_id> 3 1000` against a real active trip, confirmed 3 distinct `Sim Student N` boarding events recorded, `GET /admin/trips` correctly showed `passenger_count:3` against the vehicle's real `capacity`.
- [x] New `backend/scripts/manage-trips.ts` (`yarn trips list|vehicles|drivers|start|end|end-all`) - a terminal-only "backdoor" for trip lifecycle management, asked for directly rather than adding force-end capability to the dashboard/API. Deliberately calls `trips.model.ts`/`vehicles.model.ts`/`users.model.ts` functions directly against the DB (not over HTTP), so BE-9's driver-ownership check in `trips.service.ts` (only the driver who started a trip can end it - real, tested, intentionally left untouched) simply doesn't apply here; this script is the trusted-operator escape hatch instead of weakening that rule in the public API. `end` is idempotent (no-ops with a message if the trip isn't active rather than erroring). **Verified live**: full cycle exercised end-to-end - listed vehicles/drivers, started a trip with driver auto-picked (none given), confirmed the "vehicle already has an active trip" guard blocks a second `start` on the same vehicle, ended the trip with zero driver credentials involved, confirmed re-ending an already-completed trip no-ops cleanly, confirmed `end-all` correctly reports nothing to do when no trips are active.
- [x] New `backend/scripts/simulate-device.ts` (`yarn simulate:device`) - a single interactive "full stand-in for the physical device," combining what `simulate-taptrace.ts`, `simulate-tap.ts`, and `manage-trips.ts` each do separately into one guided flow: pick (or auto-start) a trip, choose passenger count/duration, then continuously post real GPS + boarding taps until stopped, ending the trip cleanly on exit (Ctrl+C, SIGTERM, or the configured duration elapsing). Every step - trip, vehicle, route, driver, passenger count, duration - can be given via a flag to skip its prompt, or `--yes` skips all prompts for a fully automatic run (same behavior the standalone scripts already had). Explicitly noted in its own header comment: this doesn't test a mobile UI, since mobile has no real screens built yet - it makes the backend data real/live for whichever client reads it. **Colors + interactive prompts use zero new dependencies** - `node:util`'s built-in `styleText` (confirmed this session: auto-enables real ANSI color on a TTY, auto-disables when piped/non-TTY - verified both ways) and `node:readline/promises` for numbered-menu selection and text prompts with defaults. **Verified live**: `--yes` mode run end-to-end with a 10s duration - correctly attached to an already-active trip from a prior partial run rather than erroring, set up 2 simulated passengers, ticked GPS, boarded 1 passenger (2nd tap's 8s interval fell after the 10s cutoff, correctly not fired), and cleanly ended the trip when the duration elapsed; confirmed via `yarn trips list` that no active trip was left behind. Interactive mode verified via a real pseudo-terminal (Python's `pty` module, since piped stdin alone doesn't behave like a real terminal for `readline`) - confirmed the driver-selection menu correctly processed real keystroke-style input and advanced to vehicle selection with correctly-formatted, actually-colored output (raw ANSI codes observed in the pty capture); didn't automate the full remaining flow (route/passenger-count/duration prompts) since the underlying resolution logic for all of those is identical to what `--yes` mode already fully exercised, and readline's happy-path (`rl.question()` returning typed input) doesn't vary by which prompt is showing.
- [x] Mobile app foundation + Login/Register (`PSD-101`/MOB-2): the `mobile/` Flutter app is no longer the bare scaffold. Built a full design foundation on the **green MovIO brand** (the map-pin "M" logo + the developer-supplied green ramp `#22c55e→#0f7a52`; the admin dashboard's utilitarian blue is a deliberately separate surface, and `main.dart`'s old blue seed was leftover scaffold): design tokens (`lib/core/theme/{app_colors,app_typography,app_spacing,app_radius,app_theme}.dart` — Material 3 light theme, IBM Plex Sans for UI + IBM Plex Mono for data, self-hosted/bundled fonts), shared widgets (`lib/shared/widgets/*` — branded button with inline loading, labelled text field with password toggle, logo, snackbar, a hand-rolled `ShimmerBox` skeleton + `BrandedSpinner`, no `shimmer` package), an animated brand splash (`features/splash`), and a feature-first structure mirroring the backend (`features/auth/{data,state,screens,widgets}`, `features/home`). **Auth**: `AuthRepository` → `ApiClient` (typed `{success,message,errors}` envelope handling + friendly network-failure message) → `AuthProvider` (ChangeNotifier: bootstrap/login/register/logout) → `LoginScreen`/`RegisterScreen`; JWT persisted in `flutter_secure_storage` (Keystore-backed, not plaintext prefs); `AuthGate` in `app.dart` routes splash→login→home from auth status; a nested Navigator hosts the login↔register flow so a successful auth swaps cleanly to home. **Android fixes** (would otherwise silently break on a real device): `usesCleartextTraffic="true"` (dev-only — LAN backend is `http://…:4000`, Android 9+ blocks cleartext by default), NFC `uses-feature` set to `required="false"` (only ~39% of students have NFC phones; login needs none), app label → `MovIO`. Backend base URL is compile-time (`--dart-define=API_BASE_URL=…`, default = detected LAN IP `192.168.218.233:4000`). New deps: `flutter_secure_storage` (runtime), `flutter_native_splash` + `flutter_launcher_icons` (dev, with pubspec config blocks). **Verified this session**: `flutter analyze` clean, `flutter test` green (theme token + loading-button widget test); the auth API contract verified **live** against the real backend + Supabase via curl — register (201, envelope matches `AuthUser.fromJson`), login (token), duplicate→409, wrong-password→401, validation→422 field errors — and the created `users` + `transit_wallets` rows confirmed in Supabase then cleaned up. **Not verified** (environment can't): the on-device UI, navigation, secure-storage persistence across relaunch, and the launcher-icon/native-splash generation — these need a run on the physical phone with the backend on the LAN IP (the engineer runs installs + on-device testing). Assets still to drop in: IBM Plex `.ttf` files → `mobile/assets/fonts/` (app runs without them, text falls back until present). See `docs/MovIO_Manual_Test_Plan.md` §6a.
- [x] Mobile UX overhaul: onboarding route integration + home screen enhancement + edge-to-edge system UI. **Onboarding route**: `OnboardingScreen` now wired into `AuthGate` via pattern match on `(AuthStatus.unauthenticated, false)` — first-run users see three-slide onboarding (tracking/tap-to-pay/campus-sustainability) before login; returning users skip straight to login. **Home screen redesign**: wrapped in `AppBackground` (textured paper + glows), `DoubleBackToExit` (double-tap-to-exit UX, preventing accidental close when keyboard dismiss needed), and `AnnotatedRegion` status-bar styling (dark icons on transparent bar, per-screen control). Added staggered `Entrance` animations to greeting card, wallet preview, and feature tiles (60ms increments, same entrance-animation pattern as splash/onboarding for consistency). **System UI**: `main.dart` now calls `SystemChrome.setEnabledSystemUIMode(SystemUiMode.edgeToEdge)` for full-screen layout + sets `AppSystemUi.light` as the default overlay style (each screen overrides via `AnnotatedRegion` — splash/onboarding/login = light, home = dark). **Verified**: `flutter analyze` clean; no bracket/syntax errors from prior session's wrapper edits. **Not verified**: the on-device flow splash→onboarding→login→register→home and visual/animation behavior — left to the engineer.
- [x] Live map made "really live" (backend + mobile, branch `codepraycode/psd-103-mob-5-wallet-balance-screen`): three new Socket.io broadcasts joined the existing `location:update` — `trip:started` (payload = the same row shape `GET /tracking/active` returns, location fields null until the first GPS ping), `trip:ended` (`{trip_id}`), and `trip:passengers` (`{trip_id, passenger_count, action, student_name}`) — emitted from `trips.controller.ts`/`boarding.controller.ts` via new `trips.socket.ts`/`boarding.socket.ts`. **Tap-out added without a second endpoint**: `POST /boarding/authenticate` now toggles — an open boarding session (`alighted_at IS NULL`) on that trip means tap-out (free, closes the session); otherwise tap-in (deducts 1 credit) — matching how the physical TapTrace will behave (same card, same reader). New `alighted_at TIMESTAMPTZ` column on `boarding_events` (**additive migration applied to live Supabase this session** — done directly because the running server's new queries would otherwise 500); ending a trip closes all still-open sessions in the same UPDATE statement (CTE in `trips.model.ts`). `passenger_count` semantics split deliberately: active trips = aboard right now, completed trips = total riders carried (FILTER clause in `listTripsWithPassengerCounts`; occupancy subquery in `tracking.model.ts`'s shared `ACTIVE_TRIP_SELECT`); the ridership report still counts all boardings. **`scripts/simulate-device.ts` fully rewritten** (still zero new deps): trip start/end now goes through the real REST API as a self-registered Sim Driver so the lifecycle broadcasts genuinely fire (attaching to another driver's trip falls back to direct-DB end with an explicit no-broadcast warning — the API's driver-ownership rule stays intact); GPS drives the route's actual stops JSONB at ~22 km/h (haversine, back-and-forth + jitter) instead of a random walk; and the default mode is an interactive `tap>` console (number = tap that student in/out, `a` add student, `s` status, `e` end+exit, `d` detach) — built in direct response to feedback that one-shot tapping wasn't helpful; `--auto` keeps a hands-free mode. Mobile `live_map_screen.dart` now sends the JWT in the socket handshake and listens to all three new events: `trip:started` creates the marker instantly (held pending until its first GPS fix if it has none), `trip:ended` removes it and deselects, `trip:passengers` updates the info-sheet occupancy live via a new `ActiveTrip.copyWith`; the 20s roster poll remains purely as reconciliation (and now prunes stale pending trips). **Verified live this session**: backend `tsc`/`lint` clean, `flutter analyze`/`flutter test` clean; a socket.io-client listener captured all four events with timestamps during real simulator runs (`trip:started` instantly on REST start, `trip:passengers` counts 1→2 on tap-ins and →1 on tap-out, `location:update` every ~5s, `trip:ended` instantly on REST end); DB cross-checked — credit deducted only on tap-in, `alighted_at` set on tap-out, zero open sessions left on any active trip (the 43 remaining NULLs are historical rows on completed trips predating the column). The interactive `tap>` console was also driven through a real pty end-to-end: tap-in → status shows aboard → tap-out (no charge) → `a` adds a student → tap them in → `e` ends via REST with the broadcast; no active trip left behind. **Not verified**: the mobile screen on the physical device (needs a LAN run with the simulator). See test plan §16 (LIVE-01..09). Not committed — engineer commits after their own verification.
- [ ] TapTrace firmware (parts BOM finalized/being ordered 2026-07-03 — see Open items; work still blocked until they physically arrive)
- [ ] MQTT broker (HiveMQ Cloud account not yet confirmed created)
- [ ] Manual testing (test plan written, execution not started)
- [ ] Deployment (not started)
- [ ] Chapter 4 writeup (not started)

Full task-level detail: `docs/MovIO_Milestone_Plan.md`, mirrored in Linear.
